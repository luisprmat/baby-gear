This is a Baby Gear Rental Platform, with Next.js started fresh.

Create a simple static homepage for this application. For now, make everything hard-coded, but create components were there are needed.

At the top there should be top navbar, instead of logo only text will be enough BABYGEAR, at the right corner login / register.

A very basic Hero section: heading, subtitle, CTA button linking to `/products` (that page does not exist yet).

Featured products section, will show 4 products. Product must be as a ProductCard component, which will be re-used.

Homepage must have SSR working.